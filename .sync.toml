[firsthost]
	hostname = "first"
	remote_folder = "/something/"
	rsync_options = ["--delete", "--update"]

[secondhost]
	hostname = "second"
	remote_folder = "/something/"
	rsync_options = []
	default = true
